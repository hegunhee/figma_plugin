{
  "name": "figma-mcp-bridge-plugin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "esbuild": "^0.27.3",
    "typescript": "^5.3.0"
  }
}
